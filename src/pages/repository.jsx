import Repository from '../components/userRepository'
import Container from '../components/container'
const RepositoryPage = props => (
    <Container>
        <Repository />
    </Container>
);

export default RepositoryPage;